# RoadSense AI - Machine Learning Pipeline

This directory contains the YOLOv8 pothole detection model, training scripts, and inference logic.

## Structure
- `dataset/`: Training, validation, and test images with YOLO format labels.
- `models/`: Exported `.pt` and `.onnx` model weights.
- `scripts/`: Python scripts for training and local detection.
